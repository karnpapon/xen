function Arc(Center, Radius, start, end, Direction) {
  this.x = Center.x;
  this.y = Center.y;
  this.r = Radius;
  this.xStart = start.x;
  this.yStart = start.y;
  this.xEnd = end.x;
  this.yEnd = end.y;
  this.d = Direction;
}
